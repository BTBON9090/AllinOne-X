# AllinOne Figma 插件开发复盘文档

> 本文档记录了 AllinOne 插件从立项到完成的完整开发历程，包括功能清单、技术架构、问题解决、经验总结等内容。

***

## 一、项目概况

### 1.1 项目定位

**AllinOne** 是一款面向 Figma 设计师的效率增强工具集，核心理念是"一站式解决设计工程化需求"，将高频操作、批量处理、自动化导出等功能集成在单一常驻面板中。

### 1.2 技术栈

| 层级  | 技术选型                          | 说明              |
| --- | ----------------------------- | --------------- |
| 后端  | TypeScript + Figma Plugin API | 直接操作 Figma 文档对象 |
| 前端  | HTML + CSS + JavaScript       | 单文件架构，无框架依赖     |
| 外部库 | PptxGenJS + JSZip (CDN 内联)    | PPT 生成与资源打包     |
| 构建  | NPM + TSC                     | 简单编译，无复杂打包      |

### 1.3 核心配置 (manifest.json)

```json
{
  "name": "AllinOne",
  "api": "1.0.0",
  "documentAccess": "dynamic-page",
  "networkAccess": { "allowedDomains": ["*"] }
}
```

**关键点**：`documentAccess: "dynamic-page"` 要求所有节点操作必须使用异步 API (`getNodeByIdAsync`)。

***

## 二、功能模块清单

### 2.1 简易工具 (Simple Tools)

| 功能        | 消息类型                | 说明                    |
| --------- | ------------------- | --------------------- |
| 形状转 Frame | `to-frame`          | 形状转画板，继承圆角、填充、描边、旋转   |
| Frame 转矩形 | `to-rect`           | 画板降维为矩形               |
| 互换填充描边    | `swap-fs`           | Shift+X 效果            |
| 重置图片比例    | `reset-image`       | 恢复图片原始宽高比             |
| 选中所有文本    | `select-text`       | 递归选中范围内的文本节点          |
| 移除自动布局    | `remove-al`         | 递归清除所有层级的 Auto Layout |
| 添加 AL 外套  | `add-al-wrapper`    | 为选中项添加 Auto Layout 容器 |
| 拆分文本      | `split-text`        | 按换行符拆分，保留 10px 间距     |
| 合并文本      | `join-text`         | 多文本节点合并               |
| 提升层级      | `up-one` / `up-all` | 提升一级或提升至最外层           |
| 重命名为内容    | `rename-content`    | 用文本内容重命名节点            |
| 彻底解绑实例    | `detach-all`        | 递归解绑所有嵌套组件实例          |
| 清除隐藏图层    | `remove-hidden`     | 递归删除所有隐藏节点            |
| 视觉排序图层    | `sort-layers`       | 按 Z 字型坐标重新排列图层顺序      |
| 解组全部      | `ungroup-all`       | 递归解散所有组和画板            |
| 解锁全部      | `unlock-all`        | 递归解锁所有锁定节点            |
| 像素对齐      | `pixel-perfect`     | 坐标和尺寸取整               |
| 交换位置      | `swap-positions`    | 交换两个图层的位置和尺寸          |

### 2.2 超级选择 (Selection Tools)

| 功能   | 说明                                |
| ---- | --------------------------------- |
| 范围选择 | 选中项内部、直系子级、同级、全页                  |
| 名称匹配 | 支持正则、区分大小写、全字匹配                   |
| 类型过滤 | 20+ 种图层类型（组件、画板、文本、图片等）           |
| 状态过滤 | 隐藏、锁定、蒙版、无填充、无描边等 9 种状态           |
| 属性匹配 | 宽高、坐标、透明度、填充数量等，支持 =, >, <, != 运算 |
| 拾取名称 | 一键获取当前选中项名称                       |
| 高亮定位 | `locate-node` 支持文本字符级标红           |

### 2.3 智能填充 (Smart Fill)

| 功能    | 说明                                 |
| ----- | ---------------------------------- |
| 基础填充  | 姓名、地址、手机号、邮箱、身份证、银行卡、公司名、网站、IP 等   |
| AI 生成 | 对接 OpenAI/DeepSeek 等 API，支持自定义模型配置 |
| 自定义列表 | 保存常用数据列表，支持顺序/随机录入                 |
| 高级配置  | 数值范围、日期格式、时间格式、千分位、排序方式            |

### 2.4 文字替换 (Text Replace)

| 功能   | 说明           |
| ---- | ------------ |
| 范围选择 | 选中图层 / 当前页面  |
| 查找模式 | 支持区分大小写、全字匹配 |
| 批量替换 | 一键替换所有匹配项    |
| 高亮预览 | 匹配文本字符级标红显示  |

### 2.5 样式工具 (Style Tools)

| 功能     | 消息类型            | 说明                            |
| ------ | --------------- | ----------------------------- |
| 创建本地样式 | `create-styles` | 基于选中项创建 Paint/Text/Effect 样式库 |
| 匹配本地样式 | `match-styles`  | 自动关联已存在的本地样式                  |

### 2.6 导出 PPT (PPT Master)

**核心流程（6 步引导）**：

1. **初始化**：解锁、解绑实例、移除隐藏图层、移除 Auto Layout
2. **栅格化图标**：将 `p_img` 开头的组转为 3 倍高清 PNG
3. **深度扁平化**：消除嵌套，所有元素提至 Slide Frame 直属层级
4. **生成 PPT 结构**：提取文字、形状、占位符，使用"绝对中心定位法"处理旋转
5. **导出资源包**：生成 .pptx 文件和图片素材
6. **打包下载**：合并为 `Figma_Export_Package.zip`

**支持的样式属性**：

- 文字：字体、字号、字重、颜色、行距、对齐
- 形状：圆角、描边、阴影、透明度、填充

### 2.7 组件清洗 (Variant Refiner)

| 功能          | 说明                                  |
| ----------- | ----------------------------------- |
| 智能检查 (Lint) | 组合命名法：格式 + 分隔符 + 大小写自由组合            |
| 实时预览        | 配置改动时列表实时刷新                         |
| 清洗选项        | 移除首尾空格、保留 Emoji、移除 Figma ID、标记/取消隐藏 |
| 查找替换        | 支持组件名、属性名、属性值定向替换                   |
| 三级聚合        | 组件集 → 分类 → 具体修改项                    |
| 定位功能        | 点击任意行自动锚定画布节点                       |
| 确认流         | 修复后显示 ✓ 标记，点击"继续"后清除                |

### 2.8 时空信标 (Jumpback)

| 功能   | 说明                  |
| ---- | ------------------- |
| 保存位置 | 记录当前视角、缩放、选中图层、页面信息 |
| 跨页传送 | 自动切换页面并恢复视角         |
| 命名管理 | 支持重命名锚点，方便识别        |
| 持久存储 | 数据随文件永久保存，最多 5 个    |

### 2.9 设计理论 (Design Theory)

| 功能   | 说明              |
| ---- | --------------- |
| 分类浏览 | 交互与可用性、心理学、视觉感知 |
| 搜索功能 | 支持关键词搜索（如"尼尔森"） |
| 详情展示 | 点击展开理论详情        |

***

## 三、历次问题与解决方案

### 3.1 内存溢出 (OOM)

**问题**：处理大型文件时，一次性传输过多数据导致内存溢出。

**解决方案**：

- 采用"原子化批量传输"，每批 200 个节点
- 使用"原始字节传输"而非 Base64 编码
- 在递归遍历中增加 `loopCount` 限制和 `setTimeout` 让渡

### 3.2 浏览器拦截下载

**问题**：多次触发下载被浏览器拦截。

**解决方案**：

- 将 PPT 文件和图片素材统一打包为单个 ZIP 文件下载

### 3.3 消息监听器冲突

**问题**：多个功能模块各自注册 `window.addEventListener('message')` 导致消息重复触发。

**解决方案**：

- 采用**单例监听模式**，统一使用 `window.onmessage = ...` 覆盖式写法
- 建立消息分流中心，根据 `msg.type` 前缀分发到不同处理函数

### 3.4 异步 API 兼容

**问题**：`documentAccess: "dynamic-page"` 模式下，同步 API 报错。

**解决方案**：

- 所有 `getNodeById` 升级为 `await getNodeByIdAsync`
- 页面切换使用 `await setCurrentPageAsync`

### 3.5 解绑实例崩溃

**问题**：批量解绑嵌套实例时触发 WASM 内存越界。

**解决方案**：

- 采用"迭代解绑"策略：每次只解绑一个实例，然后重新扫描
- 增加 5000 次循环熔断机制

### 3.6 面板穿透

**问题**：切换面板时，旧面板未隐藏，导致内容重叠。

**解决方案**：

- 维护完整的面板 ID 数组，切换时遍历隐藏所有面板

### 3.7 国际化动态内容

**问题**：动态生成的 DOM 元素无法响应语言切换。

**解决方案**：

- 为动态内容添加 `data-key` 属性
- `updateLanguage()` 函数遍历所有 `[data-key]` 元素更新文本

### 3.8 SVG 图标填充问题

**问题**：CSS 中 `.nav-icon { fill: currentColor }` 导致线性图标被填充。

**解决方案**：

- 为线性图标添加内联样式 `style="fill:none;stroke:currentColor"` 覆盖 CSS

***

## 四、潜在风险与注意事项

### 4.1 性能风险

- **超大型文件**：数万图层时递归遍历可能卡顿，需考虑异步批处理
- **批量修复**：超过 500 个变体时可能冻结 Figma 界面

### 4.2 兼容性风险

- **Figma API 变更**：API 升级可能导致部分功能失效
- **字体依赖**：PPT 导出依赖本地字体，跨设备可能显示异常

### 4.3 数据风险

- **锚点数据**：存储在文件中，删除文件后数据丢失
- **API Key**：存储在本地，Figma 重装后需重新配置

### 4.4 代码风险

- **单文件架构**：`code.ts` 和 `ui.html` 已超过 2000 行，维护成本增加
- **无类型检查**：`@ts-nocheck` 导致潜在类型错误被忽略

***

## 五、开发经验与方法论

### 5.1 架构设计

1. **单例监听模式**：避免多个消息监听器冲突
2. **消息分流中心**：统一入口，按类型分发
3. **防抖处理**：频繁触发的事件（如输入框）使用 debounce

### 5.2 异步处理

1. **异步优先**：Figma API 操作默认使用 async/await
2. **错误边界**：所有异步操作包裹 try-catch
3. **让渡机制**：长循环中插入 `setTimeout` 防止阻塞

### 5.3 国际化

1. **data-key 绑定**：所有需要翻译的元素添加 data-key 属性
2. **动态内容处理**：JS 生成的 DOM 也要添加 data-key
3. **双语同步**：新增文案时同时更新 zh 和 en 字典

### 5.4 调试技巧

1. **埋点日志**：关键步骤打印 `【1】【2】【3】` 标记日志
2. **错误回传**：后端错误通过 `postMessage` 回传前端显示
3. **状态重置**：失败时发送 `step-error` 消息重置 UI 按钮

### 5.5 用户体验

1. **Loading 遮罩**：耗时操作显示加载动画
2. **操作确认**：批量操作前提示用户确认
3. **错误提示**：使用 `figma.notify` 显示友好提示

***

## 六、代码规范与约定

### 6.1 命名约定

| 类型     | 约定         | 示例                                    |
| ------ | ---------- | ------------------------------------- |
| 消息类型   | 小写连字符      | `lint-variants`, `jb-save`            |
| 面板 ID  | 驼峰 + Panel | `refinerPanel`, `jumpbackPanel`       |
| i18n 键 | 下划线分隔      | `nav_simple`, `wb_btn_save_spot`      |
| 函数名    | 驼峰         | `switchGroup`, `renderRefinerResults` |

### 6.2 文件结构

```
AllinOne-X/
├── code.ts          # 后端逻辑（消息路由 + 功能实现）
├── code.js          # 编译产物
├── ui.html          # 前端界面（HTML + CSS + JS）
├── manifest.json    # 插件配置
├── package.json     # 依赖管理
└── plan.md          # 开发计划（本文档）
```

### 6.3 消息协议

```typescript
// 前端 → 后端
parent.postMessage({ 
  pluginMessage: { 
    type: 'lint-variants', 
    config: { ... } 
  } 
}, '*');

// 后端 → 前端
figma.ui.postMessage({ 
  type: 'lint-results', 
  data: [...] 
});
```

***

## 七、后续优化方向

### 7.1 性能优化

- [ ] 引入虚拟滚动（Virtual List）处理长列表
- [ ] 大数据量操作分批处理（Async Chunking）
- [ ] 缓存机制减少重复计算

### 7.2 功能增强

- [ ] PPT 字体自动映射（PingFang → 微软雅黑）
- [ ] Refiner 预设配置（一键 BEM、一键小写）
- [ ] 正则表达式高级查找
- [ ] 配置持久化（`figma.clientStorage`）

### 7.3 架构优化

- [ ] 拆分 code.ts 为模块化结构
- [ ] 引入构建工具（Vite/Webpack）
- [ ] 移除 `@ts-nocheck`，启用类型检查

### 7.4 用户体验

- [ ] 侧边栏收缩动画
- [ ] 快捷键支持
- [ ] 深色主题适配

***

## 八、致谢

本项目在开发过程中得到了多位 AI 助手的接力支持，包括但不限于：

- 架构设计与模块拆分
- 功能实现与 Bug 修复
- 国际化与 UI 优化
- 文档编写与代码审查

感谢 Figma 社区的开源精神，感谢所有贡献者的付出。

***

*文档版本：v1.0*\
*最后更新：2025-04-08*

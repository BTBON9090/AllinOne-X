{
  "name": "allinone-plugin",
  "version": "1.0.0",
  "description": "Figma All in One Plugin",
  "main": "code.js",
  "scripts": {
    "build": "tsc code.ts --outFile code.js && node -e \"const fs = require('fs'); const html = fs.readFileSync('ui.html', 'utf8'); const js = fs.readFileSync('code.js', 'utf8'); const newJs = js.replace(/__html__/g, JSON.stringify(html)); fs.writeFileSync('code.js', newJs);\"",
    "watch": "tsc code.ts --outFile code.js --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@figma/plugin-typings": "*"
  }
}